

const initialState = {
    playlist: [],
};

const playlistReducer = (state = initialState, action) => {
    switch (action.type) {
        case '':
            return {

            };
        default: 
            return null;
    }
}

export default playlistReducer;